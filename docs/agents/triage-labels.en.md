[English](triage-labels.en.md) | [简体中文](triage-labels.md)

# Triage Labels

Skills speak in five standard triage roles. This file maps those roles to the label strings actually used in this repo's issue tracker.

| Standard role     | Label in this tracker | Meaning                                 |
| ----------------- | --------------------- | --------------------------------------- |
| `needs-triage`    | `needs-triage`        | Maintainer needs to assess this issue    |
| `needs-info`      | `needs-info`          | Waiting on more info from the reporter   |
| `ready-for-agent` | `ready-for-agent`     | Spec is complete; an agent can claim it  |
| `ready-for-human` | `ready-for-human`     | Needs a human to implement               |
| `wontfix`         | `wontfix`             | Will not be addressed                    |

When a skill mentions a role (e.g. "apply the agent-ready triage label"), use the corresponding label string from this table.

Edit the right-hand column to match your actual vocabulary.
